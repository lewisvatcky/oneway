{
  "name": "oneway",
  "version": "1.0.0",
  "description": "A shipping cost estimation calculator",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lewisvatcky/oneway.git"
  },
  "author": "Lewis Vatcky",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/lewisvatcky/oneway/issues"
  },
  "homepage": "https://github.com/lewisvatcky/oneway#readme",
  "dependencies": {
    "express": "^4.16.4"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.3"
  }
}
